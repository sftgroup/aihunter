# AIHunter Redis Configuration
# Require authentication - password is set via REDIS_PASSWORD env var
# The actual password is injected by docker-compose command override
requirepass __REDIS_PASSWORD_PLACEHOLDER__
bind 0.0.0.0
protected-mode yes
maxmemory 80mb
maxmemory-policy allkeys-lru
